interface:
  display_name: "Figma to Shopify"
  short_description: "Generate and verify editable Shopify theme routes, templates, and sections."
  default_prompt: "Use $figma-to-shopify-pipeline orchestrator. It invokes phase skills in sequence: /fts-normalize, /fts-generate, /fts-preview-test, /fts-validate, /fts-push-test. Cover all routes and all completion gates."
